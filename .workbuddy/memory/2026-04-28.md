# 2026-04-28 日志

## GitHub Pages 部署完成
- 完成了上次中断的 GitHub Pages 部署
- 仓库: ninayan8229-gh/pregnancy-tracker
- 在线地址: https://ninayan8229-gh.github.io/pregnancy-tracker/
- 页面已可正常访问 (HTTP 200)

## Firebase 配置完成
- 填入了真实 Firebase 配置（apiKey、authDomain、projectId 等）
- 创建了 Realtime Database，区域选新加坡
- 正确 databaseURL: https://pregnancy-tracker-303d7-default-rtdb.asia-southeast1.firebasedatabase.app

## 多房间模式改造
- 从单房间改为多房间模式
- 每个房间有备注名（如"我自己"、"闺蜜小美"），支持创建/加入/切换/退出
- 数据结构: myRooms 数组 [{code, name, role}]
- localStorage key: pregnancy_rooms, pregnancy_current_room

## 标题修改
- 标题从"孕育时光"改为"稳稳的时光"

## 首页房间引导卡片 + 图片上传修复 + 重置功能
- 首页新增内联房间引导卡片（替代弹窗），无房间时显示
- 设置页新增"重置所有数据"按钮（红色，双确认）
- 图片上传竞态条件修复：不再依赖全局 checkedItems，直接操作 localStorage
- 添加 Cache-Control meta 标签防止缓存

## 加入房间不再要求重复输入名字
- 创建房间时将 roomName 写入 Firebase (rooms/{code}/meta/roomName)
- 加入房间时自动读取 Firebase 中的房间名，不再弹出 prompt 让用户重新输入
- 如果房间不存在名字，则用"房间 {code}"作为默认名
